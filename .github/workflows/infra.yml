# DISABLED: deployments moved to Jenkins at https://jenkins.ferredemo.dev
# This workflow is retained as a no-op so the Actions UI shows it is intentionally off.
name: Notify infra (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notify:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "Infra deploy via GitHub Actions is disabled."
          echo "Use https://jenkins.ferredemo.dev job: argus"
          exit 0
