# Platform Services

Future standalone `argus-platform` repository boundary for ingestion, workers,
WebSocket delivery, and notification runtimes.

```bash
docker compose --profile platform up --build
```
