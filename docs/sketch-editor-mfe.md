# Sketch editor MFE — out of MVP scope

The sketch / floor-plan drawing MFE (canvas editor, camera placement on plans,
ROI polygons) is **out of MVP scope** for `refactor/mvp-simplify`.

See `docs/SPEC.md` non-goals (Agent, sketch, ROI, RuleSet, Recipe, SMS,
production Auth0). MVP physical model is **Establishment + Camera** without
sketch or drawn regions.
